# 联网版使用说明

当前工程已经接入服务器端 DeepSeek 海龟汤回答方。恶魔只进行“是、不是、是也不是、与此无关”等判断，不会主动公布汤底。人工预设答案和本地关键词答案已移除：所有合格的单一是非题都由 DeepSeek 根据最新版汤底判断。DeepSeek 不可用时，该次提问不扣次数，玩家可稍后重试。

线索解锁也由 DeepSeek 进行语义判断。仅当玩家自己明确提出完整的关键命题，且该命题判定为真时，才返回对应线索 ID。只是提到某个人物或物品，不会解锁线索。

## 配置 API Key

1. 项目里已经创建 `.env.local`。
2. 打开它，把 `paste-your-key-here` 替换成自己的 DeepSeek API Key。
3. 不要将 `.env.local` 发给其他人，也不要把密钥写入 HTML 或 `app/page.tsx`。
4. 保存后必须停止并重新启动项目。

## 启动

在工程目录运行：

```bash
pnpm run dev
```

打开终端显示的本地网址即可试玩。完全离线的单 HTML 文件不能安全调用 AI，因此联网功能只存在于该工程版。

## 可调整内容

- AI 隐藏汤底与主持规则：`app/api/ask/route.ts`
- 日记、界面与搜证原型：`app/page.tsx`
- 默认模型：`.env.local` 中的 `DEEPSEEK_MODEL`
